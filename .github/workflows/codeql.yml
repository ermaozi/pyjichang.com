name: CodeQL (manual placeholder)

on:
  workflow_dispatch:

jobs:
  notice:
    name: Notice
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is manual-only
        run: |
          echo "This repository uses GitHub CodeQL Default setup."
          echo "Do not run advanced CodeQL workflow automatically together with Default setup."